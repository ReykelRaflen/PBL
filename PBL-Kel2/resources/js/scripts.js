document.getElementById('bookSubmissionForm').addEventListener('submit', function(e) {
    e.preventDefault();
    alert('Formulir berhasil dikirim!');
    // Tambahkan kode untuk mengirim data ke server
});

document.addEventListener("DOMContentLoaded", () => {
    // Add JavaScript functionality here if needed
});
